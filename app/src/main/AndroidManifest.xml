<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uniplanner">

    <application
        android:name=".UniPlanner"

        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"

        android:allowBackup="true"
        android:supportsRtl="true">

        <activity android:name=".tower.TowerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tower.LevelActivity" android:exported="true" />
        <activity android:name=".tower.RoomActivity" android:exported="true" />
        <activity android:name=".program.CourseListActivity" android:exported="true" />
        <activity android:name=".collection.CollectionActivity" android:exported="true" />
    </application>
</manifest>